---
phase: 07-player-ui-display-usage
plan: 04
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - StrataClient/Player/PlayerView.swift
  - StrataClient/Player/StemControlsView.swift
  - StrataClient/Player/TransportBarView.swift
  - StrataClient/Player/PitchPopover.swift
  - StrataClient/Player/Waveforms/WaveformsView.swift
autonomous: true
requirements:
  - DISP-05

must_haves:
  truths:
    - "PlayerView tiene layout completo: top bar + sidebar izquierda + zona principal modal + barra inferior"
    - "Sidebar muestra 4 stems (Voz, Bateria, Bajo, Otro) con boton M (mute), boton S (solo) y slider de volumen cada uno"
    - "Barra inferior tiene controles de transporte: play/pause, rew, fwd, loop, progress bar con timestamps, toggles Letras y Acordes"
    - "Barra superior tiene boton atras, titulo de cancion, boton pitch y boton A/B loop"
    - "Pitch popover muestra nota del tono actual, botones -/+ semitonos, boton restablecer"
    - "Waveforms se muestran por stem con playhead vertical sincronizado con currentTime"
    - "Cuando ningun toggle esta activo, la zona principal muestra waveforms a pantalla completa"
  artifacts:
    - path: "StrataClient/Player/PlayerView.swift"
      provides: "Vista raiz del reproductor con layout completo"
      contains: "struct PlayerView"
    - path: "StrataClient/Player/StemControlsView.swift"
      provides: "Sidebar con controles M/S/volumen por stem"
      contains: "struct StemControlsView"
    - path: "StrataClient/Player/TransportBarView.swift"
      provides: "Barra inferior con play/pause/seek/progress/toggles"
      contains: "struct TransportBarView"
    - path: "StrataClient/Player/PitchPopover.swift"
      provides: "Popover de control de pitch"
      contains: "struct PitchPopover"
    - path: "StrataClient/Player/Waveforms/WaveformsView.swift"
      provides: "4 waveforms por stem con playhead"
      contains: "WaveformView"
  key_links:
    - from: "StrataClient/Player/PlayerView.swift"
      to: "StrataClient/Player/PlayerViewModel.swift"
      via: "@Environment(PlayerViewModel.self)"
      pattern: "PlayerViewModel"
    - from: "StrataClient/Player/StemControlsView.swift"
      to: "StrataClient/Audio/PlaybackEngine.swift"
      via: "engine.setVolume/setMute/setSolo"
      pattern: "engine\\."
    - from: "StrataClient/Player/TransportBarView.swift"
      to: "StrataClient/Audio/PlaybackEngine.swift"
      via: "engine.play/pause/seek"
      pattern: "engine\\."
    - from: "StrataClient/Player/Waveforms/WaveformsView.swift"
      to: "DSWaveformImage"
      via: "WaveformView(audioURL:)"
      pattern: "WaveformView"
---

<objective>
Construir el layout completo del reproductor (modelo Moises): sidebar de stems, barra de transporte, barra superior, pitch popover, y waveforms por stem con playhead.

Purpose: Es la estructura visual principal de la app. Las vistas de letras/acordes (plan 05) se insertan en la zona principal modal.
Output: PlayerView funcional con layout completo, controles de stem, transporte, pitch y waveforms.
</objective>

<execution_context>
@/Users/danielmartorell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmartorell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-player-ui-display-usage/07-CONTEXT.md
@.planning/phases/07-player-ui-display-usage/07-RESEARCH.md
@.planning/phases/07-player-ui-display-usage/07-01-SUMMARY.md
@.planning/phases/07-player-ui-display-usage/07-03-SUMMARY.md

@StrataClient/Audio/PlaybackEngine.swift
@StrataClient/Library/CacheManager.swift
@StrataClient/Library/SongEntry.swift
@StrataClient/Player/PlayerViewModel.swift

<interfaces>
From PlaybackEngine (@Observable, @MainActor):
```swift
var currentTime: TimeInterval
var duration: TimeInterval
var isPlaying: Bool
var pitchSemitones: Int
var loopStart: TimeInterval?
var loopEnd: TimeInterval?
func play() / pause() / stop()
func seek(to time: TimeInterval)
func setPitch(semitones: Int)
func setVolume(_ volume: Float, for stem: Int)
func getVolume(for stem: Int) -> Float
func setMute(_ muted: Bool, for stem: Int)
func isMuted(_ stem: Int) -> Bool
func setSolo(_ stem: Int?)
func setLoopStart(_ time: TimeInterval?)
func setLoopEnd(_ time: TimeInterval?)
func clearLoop()
```

From PlayerViewModel (@Observable, @MainActor):
```swift
let song: SongEntry
var showTransposed: Bool
var lyrics: [LyricLine]
var chords: [ChordEntry]
var currentLine: LyricLine?
var currentWord: LyricWord?
var currentChord: ChordEntry?
var nextChord: ChordEntry?
var displayChord: String
var displayNextChord: String
func load() async throws
func savePitchOffset() async
```

Stem enum: vocals=0, drums=1, bass=2, other=3
Stem labels: ["Voz", "Bateria", "Bajo", "Otro"]

From CacheManager:
```swift
func stemURL(songID: UUID, stem: String) -> URL
// stem names: "vocals", "drums", "bass", "other"
```

DSWaveformImage (from project.yml):
```swift
import DSWaveformImageViews
WaveformView(audioURL: URL) { shape in shape.fill(Color.teal) }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PlayerView layout + StemControlsView sidebar + barra superior</name>
  <files>
    StrataClient/Player/PlayerView.swift,
    StrataClient/Player/StemControlsView.swift,
    StrataClient/Player/PitchPopover.swift
  </files>
  <action>
**PlayerView.swift** — reemplazar el placeholder del plan 03:

Layout principal con HStack (no NavigationSplitView — control total del layout):
```
┌─────────────────────────────────────────────┐
│ ← Volver  │  Titulo cancion  │ Pitch │ A/B │  ← barra superior
├────────┬────────────────────────────────────┤
│ Voz    │                                    │
│ M S ── │                                    │
│ Bat    │       zona principal               │
│ M S ── │       (waveforms / lyrics / chords)│
│ Bajo   │                                    │
│ M S ── │                                    │
│ Otro   │                                    │
│ M S ── │                                    │
├────────┴────────────────────────────────────┤
│ ◀◀  ▶/❚❚  ▶▶  🔁  ─────●───── 1:23/3:45   │  ← barra inferior
│                         Letras  Acordes     │  ← toggles
└─────────────────────────────────────────────┘
```

- `let song: SongEntry`, `let onBack: () -> Void`
- `@State private var playerVM: PlayerViewModel?`
- `@State private var showLyrics = false`
- `@State private var showChords = false`
- `@State private var showPitchPopover = false`
- `@Environment(PlaybackEngine.self) private var engine`
- `@Environment(\.cacheManager) private var cacheManager`
- `@Environment(LibraryStore.self) private var libraryStore`

Body VStack:
1. **Top bar** (HStack, padding horizontal):
   - Button "←" con `onBack()` (despues de engine.stop() y savePitchOffset)
   - Spacer
   - Text(song.title).font(.headline)
   - Spacer
   - Button pitch: muestra nota actual (derivada de ChordTransposer — tono original + pitchSemitones). Al clic: `showPitchPopover.toggle()`. `.popover(isPresented: $showPitchPopover) { PitchPopover() }`
   - Button A/B loop: iconos para setLoopStart (marca A en currentTime), setLoopEnd (marca B en currentTime), clearLoop. Toggle state visual.
2. **Divider**
3. **HStack principal**:
   - StemControlsView() — sidebar izquierda, ancho fijo ~150
   - Zona principal: Group con switch segun toggles:
     - !showLyrics && !showChords → WaveformsView(songID: song.id)
     - showLyrics && !showChords → placeholder Text("LyricsView") (plan 05 lo reemplaza)
     - showLyrics && showChords → placeholder VStack { Text("Lyrics"); Divider(); Text("Chords") }
     - !showLyrics && showChords → placeholder Text("ChordView")
4. **TransportBarView(showLyrics: $showLyrics, showChords: $showChords)** — barra inferior

.task: crear PlayerViewModel con song, engine, cacheManager!, libraryStore. Llamar vm.load(). Asignar a playerVM. Pasar como .environment(playerVM!) al contenido.

**StemControlsView.swift:**
- `@Environment(PlaybackEngine.self) private var engine`
- VStack con 4 stem rows, orden fijo: Voz(0), Bateria(1), Bajo(2), Otro(3)
- Cada stem row:
  - Text(label).font(.caption).frame(maxWidth: .infinity, alignment: .leading)
  - HStack: Button "M" (toggle mute, highlight cuando muted), Button "S" (toggle solo, highlight cuando soloed)
  - Slider(value:) 0...1 para volumen, binding via engine.setVolume/getVolume
- Ancho fijo del sidebar: .frame(width: 140)
- Divider vertical entre sidebar y zona principal

**PitchPopover.swift:**
- `@Environment(PlaybackEngine.self) private var engine`
- `@Environment(PlayerViewModel.self) private var vm` (si disponible) para savePitchOffset
- VStack:
  - Text "Tono" .font(.caption) .foregroundStyle(.secondary)
  - Text con nota actual en grande: si song.key existe, transponer con engine.pitchSemitones. Sino mostrar solo el offset "+N" o "-N"
  - HStack: Button "−" (engine.setPitch(semitones: pitchSemitones - 1)), Text("\(engine.pitchSemitones)"), Button "+" (engine.setPitch(semitones: pitchSemitones + 1))
  - Button "Restablecer" { engine.setPitch(semitones: 0) }
  - Si vm.chords no vacio: Toggle "Mostrar transpuesto" bound to vm.showTransposed
- Al cerrar popover o cambiar pitch: llamar vm.savePitchOffset()

Seguir esquema de colores del sistema (light/dark) — NO forzar modo oscuro.
  </action>
  <verify>
    <automated>cd /Volumes/T7_SAMSUNG/strata && xcodegen generate 2>&1 && xcodebuild -scheme StrataClient -destination 'platform=macOS' build 2>&1 | tail -5</automated>
  </verify>
  <done>PlayerView muestra layout completo con sidebar de stems, barra superior con titulo/pitch/A-B, zona principal, y slots para toggles. PitchPopover funciona con -/+/restablecer y toggle transpuesto.</done>
</task>

<task type="auto">
  <name>Task 2: TransportBarView + WaveformsView</name>
  <files>
    StrataClient/Player/TransportBarView.swift,
    StrataClient/Player/Waveforms/WaveformsView.swift
  </files>
  <action>
**TransportBarView.swift:**
- `@Environment(PlaybackEngine.self) private var engine`
- `@Binding var showLyrics: Bool`
- `@Binding var showChords: Bool`
- Layout VStack con Divider arriba:
  1. **Progress bar**: Slider(value: binding) de 0 a engine.duration. El binding: get { engine.currentTime }, set { engine.seek(to: $0) }. Timestamps a los lados: Text(formatTime(engine.currentTime)) y Text(formatTime(engine.duration))
  2. **Controles HStack centrado**:
     - Button retroceso 10s: `engine.seek(to: max(0, engine.currentTime - 10))`
     - Button play/pause: icono condicional "play.fill" / "pause.fill", accion engine.isPlaying ? engine.pause() : engine.play(). Tamano mas grande (.font(.title))
     - Button avance 10s: `engine.seek(to: min(engine.duration, engine.currentTime + 10))`
     - Button loop: icono "repeat" con highlight si loopStart != nil && loopEnd != nil
     - Spacer
     - Toggle buttons (estilo boton, no switch): "Letras" bound to showLyrics, "Acordes" bound to showChords. Highlight visual cuando activo (tint o background).
     - NO incluir toggle "Secciones" — diferido a v2 per user decision.
- `private func formatTime(_ seconds: TimeInterval) -> String`: formato "m:ss"

**WaveformsView.swift** — crear StrataClient/Player/Waveforms/WaveformsView.swift:
- `import DSWaveformImageViews`
- `struct WaveformsView: View`
  - `let songID: UUID`
  - `@Environment(PlaybackEngine.self) private var engine`
  - `@Environment(\.cacheManager) private var cacheManager`
- Body: VStack con 4 StemWaveformRow, una por stem en orden: vocals, drums, bass, other.
- Cada row expandida equitativamente (frame maxHeight .infinity).

**StemWaveformRow** (en mismo archivo o subview):
- `let stemURL: URL`
- `let stemLabel: String`
- `@Environment(PlaybackEngine.self) private var engine`
- Body: GeometryReader con ZStack alignment .leading:
  1. `WaveformView(audioURL: stemURL) { shape in shape.fill(Color.teal.opacity(0.7)) }` — color teal para todas las pistas (Claude's discretion)
  2. Playhead: Rectangle().fill(Color.primary).frame(width: 2).offset(x: engine.duration > 0 ? geo.size.width * CGFloat(engine.currentTime / engine.duration) : 0)
- Label: overlay Text(stemLabel) .font(.caption2) .foregroundStyle(.secondary) alineado top-leading
- Frame height: no fijar — dejar que el VStack lo distribuya equitativamente

Nota: Las URLs de stems se obtienen de cacheManager. Construir en WaveformsView:
```swift
let stems = ["vocals", "drums", "bass", "other"]
let labels = ["Voz", "Bateria", "Bajo", "Otro"]
// cacheManager!.stemURL(songID: songID, stem: stemName)
```
Como cacheManager es un actor, llamar stemURL es sincrono (no async) — es solo una concatenacion de paths.
Verificar si stemURL es nonisolated o no. Si es async, precomputar las URLs en .task o en init.
  </action>
  <verify>
    <automated>cd /Volumes/T7_SAMSUNG/strata && xcodegen generate 2>&1 && xcodebuild -scheme StrataClient -destination 'platform=macOS' build 2>&1 | tail -5</automated>
  </verify>
  <done>TransportBarView muestra progress bar, play/pause/rew/fwd/loop, toggles Letras/Acordes (sin Secciones). WaveformsView muestra 4 waveforms con playhead sincronizado con currentTime.</done>
</task>

</tasks>

<verification>
- PlayerView layout completo visible: sidebar + zona principal + barras
- Stem controls mutan/silencian/solo stems via PlaybackEngine
- Progress bar refleja currentTime y permite seek
- Play/pause funciona
- Pitch popover muestra -/+/restablecer
- Waveforms renderizan desde archivos WAV del cache
- Playhead se mueve con currentTime
- Build compila sin errores
</verification>

<success_criteria>
El reproductor es visualmente completo y funcional: el usuario puede controlar la reproduccion, ajustar stems, cambiar pitch, ver waveforms con playhead. Solo faltan letras y acordes (plan 05).
</success_criteria>

<output>
After completion, create `.planning/phases/07-player-ui-display-usage/07-04-SUMMARY.md`
</output>
