---
phase: 07-player-ui-display-usage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/usage/tracker.py
  - server/app.py
  - StrataClient/Import/ImportViewModel.swift
autonomous: true
requirements:
  - USGR-03
  - USGR-04

must_haves:
  truths:
    - "El servidor devuelve 429 cuando el coste estimado del mes actual supera SPENDING_LIMIT_USD antes de lanzar el pipeline GPU"
    - "El cliente muestra un mensaje claro cuando recibe 429 sin bloquear la reproduccion de canciones ya procesadas"
  artifacts:
    - path: "server/usage/tracker.py"
      provides: "check_limit(username) que devuelve True si el limite esta alcanzado"
      contains: "def check_limit"
    - path: "server/app.py"
      provides: "Check de limite antes de lanzar pipeline GPU"
      contains: "429"
    - path: "StrataClient/Import/ImportViewModel.swift"
      provides: "Captura de 429 HTTP con mensaje al usuario"
      contains: "429"
  key_links:
    - from: "server/app.py"
      to: "server/usage/tracker.py"
      via: "check_limit() llamado antes de process"
      pattern: "check_limit"
    - from: "StrataClient/Import/ImportViewModel.swift"
      to: "APIError.rateLimited o httpError(429)"
      via: "catch en runFileImport/runURLImport"
      pattern: "429\\|rateLimited"
---

<objective>
Implementar el limite mensual de gasto: el servidor rechaza con 429 cuando se supera el presupuesto, y el cliente muestra un banner informativo.

Purpose: USGR-04 requiere que el servidor bloquee procesamiento al alcanzar el limite. USGR-03 ya esta parcialmente implementado (tracking), falta el enforcement.
Output: Logica de 429 en server + manejo en cliente.
</objective>

<execution_context>
@/Users/danielmartorell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmartorell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-player-ui-display-usage/07-CONTEXT.md

@server/usage/tracker.py
@server/app.py
@StrataClient/Import/ImportViewModel.swift
@StrataClient/Import/ImportView.swift
@StrataClient/Network/APIError.swift

<interfaces>
From server/usage/tracker.py:
```python
SPENDING_LIMIT_USD = 10.00
GPU_SECONDS_PER_SONG = 45
MODAL_T4_RATE_PER_SECOND = 0.000164

def get_usage(username: str) -> dict:
    # Returns: {"month", "songs_processed", "gpu_seconds", "estimated_cost_usd", "spending_limit_usd"}

def record_usage(username: str, source_type: str, source_name: str) -> None:
```

From server/app.py process endpoints:
```python
# process_file y process_url llaman a self.process() internamente
# Ambos validan JWT con require_auth antes de procesar
```

From ImportViewModel:
```swift
private func runFileImport(fileURL: URL) async { ... }
private func runURLImport(urlString: String) async { ... }
// Ambos capturan errores en catch { phase = .error(error.localizedDescription) }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server — check_limit() + 429 en endpoints de proceso</name>
  <files>server/usage/tracker.py, server/app.py</files>
  <action>
**tracker.py:**
- Anadir funcion `def check_limit(username: str) -> bool`:
  1. Leer usage.json via _read_usage()
  2. Obtener mes actual
  3. Calcular estimated_cost_usd del mes
  4. Retornar True si estimated_cost_usd >= SPENDING_LIMIT_USD

**app.py:**
- En el web handler de FastAPI (la funcion que maneja POST /process-file y POST /process-url), ANTES de hacer spawn al pipeline GPU:
  1. Importar check_limit desde usage.tracker
  2. Llamar check_limit(username)
  3. Si retorna True, devolver `JSONResponse(status_code=429, content={"detail": "Monthly spending limit reached"})`
- El check debe ocurrir en los handlers HTTP, no dentro de process() (que corre en GPU).
  Buscar las funciones que manejan /process-file y /process-url en la seccion web() de app.py y anadir el check al principio de cada una, despues de la autenticacion pero antes del spawn.

NO modificar process_job ni la logica GPU. El 429 se devuelve desde el web handler HTTP.
  </action>
  <verify>
    <automated>cd /Volumes/T7_SAMSUNG/strata && python3 -c "import ast; ast.parse(open('server/app.py').read()); ast.parse(open('server/usage/tracker.py').read()); print('OK')"</automated>
  </verify>
  <done>check_limit() existe en tracker.py. Los endpoints /process-file y /process-url devuelven 429 antes de spawnar GPU cuando el limite esta alcanzado.</done>
</task>

<task type="auto">
  <name>Task 2: Cliente — capturar 429 en ImportViewModel con mensaje claro</name>
  <files>StrataClient/Import/ImportViewModel.swift</files>
  <action>
**ImportViewModel.swift:**
- En runFileImport y runURLImport, el error ya se captura como `catch { phase = .error(error.localizedDescription) }`.
- El APIClient lanza APIError.httpError(statusCode) para respuestas no-200. Necesitamos detectar 429 especificamente.
- En ambos metodos (runFileImport, runURLImport), en el bloque catch, anadir un check ANTES del catch generico:
  ```swift
  } catch let error as APIError where error == .httpError(429) {
      phase = .error("Limite mensual de procesamiento alcanzado. Puedes seguir reproduciendo canciones ya procesadas.")
  } catch is CancellationError {
      phase = .idle
  } catch {
      phase = .error(error.localizedDescription)
  }
  ```
- Alternativamente, si Plan 01 ya ha anadido APIError.rateLimited, mapear httpError(429) a rateLimited en el APIClient y capturar ese caso aqui. Si Plan 01 no se ha ejecutado aun (wave 1 paralelo), usar el patron httpError(429) directamente que ya funciona sin cambios en APIError.

Nota: Este plan es wave 1 paralelo con Plan 01. No asumir que APIError.rateLimited existe. Usar `.httpError(429)` que ya compila.
  </action>
  <verify>
    <automated>cd /Volumes/T7_SAMSUNG/strata && xcodegen generate 2>&1 && xcodebuild -scheme StrataClient -destination 'platform=macOS' build 2>&1 | tail -5</automated>
  </verify>
  <done>ImportViewModel muestra "Limite mensual de procesamiento alcanzado..." cuando recibe 429 del servidor. El usuario puede seguir reproduciendo canciones existentes.</done>
</task>

</tasks>

<verification>
- tracker.py tiene check_limit() que lee usage y compara con SPENDING_LIMIT_USD
- app.py devuelve 429 en /process-file y /process-url cuando check_limit retorna True
- ImportViewModel captura 429 con mensaje descriptivo en espanol
- Python files parsean sin errores de sintaxis
- Swift compila sin errores
</verification>

<success_criteria>
El servidor bloquea nuevos procesamientos cuando el gasto mensual supera $10 y el cliente comunica esto al usuario sin bloquear la app.
</success_criteria>

<output>
After completion, create `.planning/phases/07-player-ui-display-usage/07-02-SUMMARY.md`
</output>
