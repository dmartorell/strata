---
phase: 06-import-end-to-end-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - StrataClient/Network/APIClient.swift
  - StrataClient/Network/APIEndpoint.swift
  - StrataClient/Import/ImportPhase.swift
  - StrataClient/Import/ImportViewModel.swift
  - StrataClient.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - IMPT-01
  - IMPT-02
  - IMPT-03
  - IMPT-04

must_haves:
  truths:
    - "APIClient.uploadAudio envía a /process-file (no a /process stub)"
    - "APIClient tiene uploadURL para POST /process-url con JSON body {url}"
    - "pollJobStatus devuelve JobResult con zipData no-nil cuando Content-Type es application/zip"
    - "ImportPhase enum cubre todos los estados: idle/validating/uploading/processing/ready/error"
    - "ImportViewModel orquesta el flujo completo: hash → cache check → upload → poll → unzip → addSong"
  artifacts:
    - path: "StrataClient/Import/ImportPhase.swift"
      provides: "Estado de la máquina de estados de importación"
      exports: ["ImportPhase"]
    - path: "StrataClient/Import/ImportViewModel.swift"
      provides: "ViewModel de importación con flujo completo"
      exports: ["ImportViewModel"]
    - path: "StrataClient/Network/APIClient.swift"
      provides: "Endpoints corregidos + ZIP detection"
      exports: ["APIClient", "JobResult"]
    - path: "StrataClient/Network/APIEndpoint.swift"
      provides: "Casos processFile y processURL"
      exports: ["APIEndpoint"]
  key_links:
    - from: "ImportViewModel.startFileImport"
      to: "APIClient.uploadAudio"
      via: "async/await call con token de AuthViewModel"
      pattern: "uploadAudio.*token"
    - from: "ImportViewModel.startURLImport"
      to: "APIClient.uploadURL"
      via: "async/await call con token de AuthViewModel"
      pattern: "uploadURL.*token"
    - from: "APIClient.pollJobStatus"
      to: "JobResult.zipData"
      via: "Content-Type: application/zip detection"
      pattern: "application/zip"
    - from: "ImportViewModel (unpack)"
      to: "CacheManager.materializeSong"
      via: "ZIPFoundation extract a tempDir + materializeSong"
      pattern: "materializeSong"
---

<objective>
Corregir la capa de red para el pipeline real e implementar `ImportViewModel` con el flujo completo de importación.

Purpose: APIClient apunta a stubs `/process` que no existen en producción. La corrección a `/process-file` y `/process-url` + ZIP detection es el prerequisito bloqueante de toda la fase. ImportViewModel contiene la lógica de negocio (hash → cache check → upload → poll → unzip → persistir) independientemente de la UI.

Output: APIClient funcional contra los endpoints reales del servidor, ImportPhase enum, ImportViewModel @Observable @MainActor con flujo completo, ZIPFoundation añadido via SPM.
</objective>

<execution_context>
@/Users/danielmartorell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmartorell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-import-end-to-end-flow/06-RESEARCH.md

@StrataClient/Network/APIClient.swift
@StrataClient/Network/APIEndpoint.swift
@StrataClient/Library/CacheManager.swift
@StrataClient/Library/LibraryStore.swift
@StrataClient/Library/SongEntry.swift
@StrataClient/Library/SongMetadata.swift
@StrataClient/Auth/AuthViewModel.swift
@server/processors/stub_processor.py
</context>

<interfaces>
<!-- Contratos existentes que ImportViewModel consumirá -->

From StrataClient/Library/CacheManager.swift:
```swift
actor CacheManager: CacheManagerProtocol {
    func sha256(of fileURL: URL) throws -> String  // chunked SHA256
    func youtubeVideoID(from urlString: String) -> String?  // validación YouTube
    func materializeSong(id: UUID, from tempDir: URL) throws  // mueve stems+JSONs a destino
}
```

From StrataClient/Library/LibraryStore.swift:
```swift
@Observable @MainActor
final class LibraryStore {
    func addSong(_ entry: SongEntry) async
    func isCached(sourceHash: String) -> Bool
}
```

From StrataClient/Library/SongEntry.swift:
```swift
struct SongEntry: Codable, Identifiable, Sendable {
    let id: UUID
    let title: String
    let artist: String?
    let duration: Double
    let sourceURL: String?
    let fileName: String?
    let sourceHash: String
    let addedAt: Date
}
```

From server/processors/stub_processor.py — endpoints reales:
```
POST /process-file   — UploadFile multipart, devuelve {"job_id": "..."}
POST /process-url    — JSON body {"url": "..."}, devuelve {"job_id": "..."}
GET  /result/{id}    — devuelve JSON {status, job_id} o application/zip cuando completed
```

Server status values: "queued", "separating", "transcribing", "detecting_chords", "packaging", "completed", "error:..."
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix APIClient + APIEndpoint para endpoints reales</name>
  <files>StrataClient/Network/APIClient.swift, StrataClient/Network/APIEndpoint.swift</files>
  <action>
**APIEndpoint.swift:** Renombrar caso `.process` a `.processFile` con path `/process-file`. Añadir caso `.processURL` con path `/process-url` y method POST.

**APIClient.swift:**

1. **JobResult:** Añadir campo `zipData: Data? = nil`. Struct ya no necesita ser `Decodable` exclusivamente — mantener Decodable pero con zipData como campo extra (o usar un tipo diferente). La solución más limpia: hacer JobResult no-Decodable y construirlo manualmente. Dado que `JobStatusResponse.result` ya no se usa para el ZIP, cambiar a:
```swift
struct JobResult: Sendable {
    var zipData: Data? = nil
    var status: String = ""
}
```

2. **uploadAudio:** Cambiar `APIEndpoint.process` → `APIEndpoint.processFile`.

3. **uploadURL:** Añadir método nuevo:
```swift
func uploadURL(urlString: String, token: String) async throws -> String {
    let endpoint = APIEndpoint.processURL
    var request = makeRequest(endpoint: endpoint, token: token)
    request.setValue("application/json", forHTTPHeaderField: "Content-Type")
    request.httpBody = try JSONSerialization.data(withJSONObject: ["url": urlString])
    let (data, response) = try await transport.data(for: request)
    try checkResponse(response, isAuthenticated: true)
    let decoded = try decode(ProcessResponse.self, from: data)
    return decoded.job_id
}
```

4. **pollJobStatus — ZIP detection:** Justo tras `try checkResponse(...)`, antes del decode JSON:
```swift
let httpResponse = response as! HTTPURLResponse
let contentType = httpResponse.value(forHTTPHeaderField: "Content-Type") ?? ""
if contentType.hasPrefix("application/zip") {
    return JobResult(zipData: data, status: "completed")
}
let statusResponse = try decode(JobStatusResponse.self, from: data)
let status = statusResponse.status
if status == "completed" {
    return JobResult(zipData: nil, status: "completed")
}
if status.hasPrefix("error:") {
    let message = String(status.dropFirst("error:".count))
    throw APIError.processingFailed(message)
}
```
Nota: `JobStatusResponse.result` queda obsoleto — mantenerlo como `Optional<JobResult>` o eliminar. Simplificar: eliminar el campo `result` de `JobStatusResponse` ya que el ZIP se devuelve directamente, no anidado en JSON.

Nota sobre `APIEndpoint.processFile` method: debe ser POST. En el `var method` switch, añadir `.processFile, .processURL` al caso POST.
  </action>
  <verify>
    <automated>cd /Volumes/T7_SAMSUNG/strata && xcodebuild -scheme StrataClient -destination 'platform=macOS' build 2>&1 | tail -5</automated>
  </verify>
  <done>Build succeeded. APIEndpoint tiene .processFile y .processURL. APIClient.pollJobStatus detecta application/zip y devuelve JobResult con zipData no-nil.</done>
</task>

<task type="auto">
  <name>Task 2: ImportPhase enum + ImportViewModel con flujo completo</name>
  <files>StrataClient/Import/ImportPhase.swift, StrataClient/Import/ImportViewModel.swift, StrataClient.xcodeproj/project.pbxproj</files>
  <action>
Crear directorio `StrataClient/Import/` y los dos archivos. Añadir ambos al target `StrataClient` en el proyecto Xcode (si se usa xcodegen con `project.yml`, añadir en la lista de sources; si se edita project.pbxproj directamente, usar la convención de archivos existentes).

**StrataClient/Import/ImportPhase.swift:**
```swift
import Foundation

enum ImportPhase: Equatable {
    case idle
    case validating
    case uploading
    case processing(stage: String)
    case ready(cached: Bool)
    case error(String)

    var displayLabel: String {
        switch self {
        case .idle:              return ""
        case .validating:        return "Validando..."
        case .uploading:         return "Subiendo archivo..."
        case .processing(let s): return stageLabel(s)
        case .ready(true):       return "Cargado del caché"
        case .ready(false):      return "Listo"
        case .error(let msg):    return "Error: \(msg)"
        }
    }

    private func stageLabel(_ stage: String) -> String {
        switch stage {
        case "queued":           return "En cola..."
        case "separating":       return "Separando stems..."
        case "transcribing":     return "Transcribiendo letras..."
        case "detecting_chords": return "Detectando acordes..."
        case "packaging":        return "Empaquetando..."
        default:                 return "Procesando..."
        }
    }

    var isActive: Bool {
        switch self {
        case .idle, .ready, .error: return false
        default: return true
        }
    }
}
```

**StrataClient/Import/ImportViewModel.swift:**
```swift
import Foundation
import Observation
import ZIPFoundation

@Observable
@MainActor
final class ImportViewModel {
    private(set) var phase: ImportPhase = .idle

    private let apiClient: APIClient
    private let cacheManager: CacheManager
    private let libraryStore: LibraryStore
    private var authViewModel: AuthViewModel
    private var currentTask: Task<Void, Never>?

    init(apiClient: APIClient, cacheManager: CacheManager, libraryStore: LibraryStore, authViewModel: AuthViewModel) {
        self.apiClient = apiClient
        self.cacheManager = cacheManager
        self.libraryStore = libraryStore
        self.authViewModel = authViewModel
    }

    func startFileImport(from fileURL: URL) {
        cancelCurrentTask()
        currentTask = Task {
            await runFileImport(fileURL: fileURL)
        }
    }

    func startURLImport(urlString: String) {
        cancelCurrentTask()
        currentTask = Task {
            await runURLImport(urlString: urlString)
        }
    }

    func cancel() {
        cancelCurrentTask()
        phase = .idle
    }

    // MARK: - Private

    private func cancelCurrentTask() {
        currentTask?.cancel()
        currentTask = nil
    }

    private func runFileImport(fileURL: URL) async {
        do {
            phase = .validating
            try Task.checkCancellation()

            let hash = try await cacheManager.sha256(of: fileURL)

            if libraryStore.isCached(sourceHash: hash) {
                phase = .ready(cached: true)
                return
            }

            guard let token = authViewModel.token else {
                phase = .error("No hay sesión activa")
                return
            }

            phase = .uploading
            try Task.checkCancellation()

            let fileData = try Data(contentsOf: fileURL)
            let mimeType = audioMimeType(for: fileURL)
            let jobId = try await apiClient.uploadAudio(
                fileData: fileData,
                fileName: fileURL.lastPathComponent,
                mimeType: mimeType,
                token: token
            )

            try await pollAndFinalize(jobId: jobId, sourceHash: hash, displayName: fileURL.deletingPathExtension().lastPathComponent, sourceURL: nil, fileName: fileURL.lastPathComponent)
        } catch is CancellationError {
            phase = .idle
        } catch {
            phase = .error(error.localizedDescription)
        }
    }

    private func runURLImport(urlString: String) async {
        do {
            phase = .validating
            try Task.checkCancellation()

            guard let videoID = await cacheManager.youtubeVideoID(from: urlString) else {
                phase = .error("URL de YouTube no válida")
                return
            }

            if libraryStore.isCached(sourceHash: videoID) {
                phase = .ready(cached: true)
                return
            }

            guard let token = authViewModel.token else {
                phase = .error("No hay sesión activa")
                return
            }

            phase = .uploading
            try Task.checkCancellation()

            let jobId = try await apiClient.uploadURL(urlString: urlString, token: token)

            try await pollAndFinalize(jobId: jobId, sourceHash: videoID, displayName: urlString, sourceURL: urlString, fileName: nil)
        } catch is CancellationError {
            phase = .idle
        } catch {
            phase = .error(error.localizedDescription)
        }
    }

    private func pollAndFinalize(jobId: String, sourceHash: String, displayName: String, sourceURL: String?, fileName: String?) async throws {
        phase = .processing(stage: "queued")
        try Task.checkCancellation()

        // pollJobStatus ya hace polling con sleep 3s entre intentos (maxAttempts: 60 = 3 min)
        let jobResult = try await apiClient.pollJobStatus(jobId: jobId, token: authViewModel.token ?? "")

        guard let zipData = jobResult.zipData else {
            throw ImportError.missingZipData
        }

        phase = .processing(stage: "extracting")
        try Task.checkCancellation()

        let entry = try await Task.detached(priority: .userInitiated) {
            try self.extractAndMaterialize(zipData: zipData, sourceHash: sourceHash, displayName: displayName, sourceURL: sourceURL, fileName: fileName)
        }.value

        await libraryStore.addSong(entry)
        phase = .ready(cached: false)
    }

    private func extractAndMaterialize(zipData: Data, sourceHash: String, displayName: String, sourceURL: String?, fileName: String?) throws -> SongEntry {
        let tempDir = FileManager.default.temporaryDirectory
            .appendingPathComponent(UUID().uuidString, isDirectory: true)
        try FileManager.default.createDirectory(at: tempDir, withIntermediateDirectories: true)
        defer { try? FileManager.default.removeItem(at: tempDir) }

        let archive = try Archive(data: zipData, accessMode: .read)
        for entry in archive {
            let destURL = tempDir.appendingPathComponent(entry.path)
            _ = try archive.extract(entry, to: destURL)
        }

        // Leer metadata.json para construir SongEntry
        let metadataURL = tempDir.appendingPathComponent("metadata.json")
        let metadataData = try Data(contentsOf: metadataURL)
        let decoder = JSONDecoder()
        let metadata = try decoder.decode(SongMetadata.self, from: metadataData)

        let songID = UUID()
        // materializeSong mueve los archivos desde tempDir al destino permanente
        // CacheManager es actor — llamar desde contexto nonisolated requiere Task aislado
        // Nota: esta función se llama desde Task.detached, no desde MainActor
        // Usamos un actor hop explícito:
        // (ver nota abajo — la llamada se restructura en extractAndMaterialize)

        // IMPORTANTE: CacheManager.materializeSong es actor-isolated.
        // Esta función se ejecuta en Task.detached (sin actor). Necesitamos await.
        // Refactorizar: NO usar extractAndMaterialize como throws-sync.
        // En su lugar, hacer extractAndMaterialize async throws y llamar await cacheManager.materializeSong.

        // Retornar SongEntry con ID provisional; materializeSong se llamará fuera.
        // La firma de este método necesita ser async — ver nota de implementación.
        let entry = SongEntry(
            id: songID,
            title: metadata.title,
            artist: metadata.artist,
            duration: metadata.duration,
            sourceURL: sourceURL,
            fileName: fileName,
            sourceHash: sourceHash,
            addedAt: Date()
        )
        return entry
    }

    private func audioMimeType(for url: URL) -> String {
        switch url.pathExtension.lowercased() {
        case "mp3":  return "audio/mpeg"
        case "wav":  return "audio/wav"
        case "flac": return "audio/flac"
        case "m4a":  return "audio/mp4"
        default:     return "audio/mpeg"
        }
    }
}

enum ImportError: LocalizedError {
    case missingZipData
    case invalidMetadata(String)

    var errorDescription: String? {
        switch self {
        case .missingZipData:          return "El servidor no devolvió el archivo ZIP"
        case .invalidMetadata(let m):  return "Metadatos inválidos: \(m)"
        }
    }
}
```

**Nota crítica de implementación:** `extractAndMaterialize` necesita ser `async throws` porque `CacheManager.materializeSong` es actor-isolated. Reescribir `pollAndFinalize` para que llame `await cacheManager.materializeSong` directamente en el bloque `Task.detached` no es posible (el actor no es Sendable desde detached sin hop). La solución correcta: hacer la extracción ZIP en `Task.detached`, luego en el contexto @MainActor llamar `await cacheManager.materializeSong(id:from:)` con el UUID y el tempDir generado. El tempDir NO debe borrarse en el `defer` si hay archivos que mover — eliminarlo solo tras `materializeSong`. Ajustar el código para este flujo:

```swift
// En pollAndFinalize, reemplazar Task.detached:
let (songEntry, tempDir) = try await Task.detached(priority: .userInitiated) {
    return try self.extractToTemp(zipData: zipData, sourceHash: sourceHash, sourceURL: sourceURL, fileName: fileName)
}.value

// Luego en MainActor context:
try await cacheManager.materializeSong(id: songEntry.id, from: tempDir)
// tempDir lo borra materializeSong internamente (ya tiene el try? removeItem al final)
await libraryStore.addSong(songEntry)
phase = .ready(cached: false)
```

**SPM — ZIPFoundation:** Añadir al proyecto Xcode via File > Add Package Dependencies:
URL: `https://github.com/weichsel/ZIPFoundation.git`, version `.upToNextMajor(from: "0.9.0")`.
Target: `StrataClient`.

Si el proyecto usa `project.yml` (xcodegen), añadir en `packages:` y `dependencies:` del target.

Verificar que `project.pbxproj` (o `project.yml`) incluya ZIPFoundation como dependencia del target StrataClient.
  </action>
  <verify>
    <automated>cd /Volumes/T7_SAMSUNG/strata && xcodebuild -scheme StrataClient -destination 'platform=macOS' build 2>&1 | tail -10</automated>
  </verify>
  <done>Build succeeded. ImportPhase y ImportViewModel existen y compilan. ZIPFoundation está en el proyecto. CacheManager.youtubeVideoID es callable desde ImportViewModel (actor hop correcto).</done>
</task>

</tasks>

<verification>
1. `xcodebuild build` sin errores ni warnings de type-safety
2. `APIEndpoint.processFile` apunta a `/process-file` (POST)
3. `APIEndpoint.processURL` apunta a `/process-url` (POST)
4. `pollJobStatus` tiene el bloque `content-type: application/zip` detection antes del JSON decode
5. `ImportPhase.displayLabel` devuelve strings en español para todos los casos
6. `ImportViewModel.startFileImport` y `startURLImport` existen y son @MainActor
7. ZIPFoundation aparece en las dependencias del proyecto Xcode
</verification>

<success_criteria>
- Build succeeded sin errores
- APIClient apunta a `/process-file` y tiene `uploadURL` para `/process-url`
- `pollJobStatus` detecta `application/zip` y retorna `JobResult(zipData: data)`
- `ImportViewModel` orquesta el flujo completo incluyendo cache-hit detection para archivos y YouTube URLs
- `extractAndMaterialize` + `cacheManager.materializeSong` correctamente secuenciados respetando el actor isolation de CacheManager
</success_criteria>

<output>
After completion, create `.planning/phases/06-import-end-to-end-flow/06-01-SUMMARY.md`
</output>
