---
phase: 06-import-end-to-end-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - StrataClient/Import/ImportView.swift
  - StrataClient/ContentView.swift
  - StrataClient/App/StrataApp.swift
autonomous: false
requirements:
  - IMPT-01
  - IMPT-02
  - IMPT-03
  - IMPT-04

must_haves:
  truths:
    - "El usuario puede arrastrar un MP3/WAV/FLAC/M4A a la ventana y el import arranca automáticamente"
    - "El usuario puede hacer clic en 'Pegar URL de YouTube' y el import arranca si el portapapeles tiene una URL válida"
    - "La UI muestra el label de progreso actualizado en tiempo real (validando → subiendo → procesando → listo)"
    - "Si el import termina con error, la UI muestra el mensaje de error y permite reintentar"
    - "El botón cancelar detiene el import en cualquier punto"
  artifacts:
    - path: "StrataClient/Import/ImportView.swift"
      provides: "Drop zone + paste button + progress UI"
      exports: ["ImportView"]
    - path: "StrataClient/ContentView.swift"
      provides: "Layout principal con ImportView + library list"
      exports: ["ContentView"]
    - path: "StrataClient/App/StrataApp.swift"
      provides: "ImportViewModel wired en el environment"
      exports: ["StrataApp"]
  key_links:
    - from: "ImportView.onDrop"
      to: "ImportViewModel.startFileImport"
      via: "NSItemProvider.loadFileRepresentation → Task @MainActor"
      pattern: "startFileImport"
    - from: "ImportView paste button"
      to: "ImportViewModel.startURLImport"
      via: "NSPasteboard.general.string + youtubeVideoID validation"
      pattern: "startURLImport"
    - from: "ImportView"
      to: "ImportViewModel.phase"
      via: "@Environment(importViewModel).phase"
      pattern: "importViewModel.phase"
---

<objective>
Construir la UI de importación (drag & drop + paste + progress) y conectarla a ImportViewModel.

Purpose: ImportViewModel ya tiene la lógica (Plan 01). Este plan añade la capa visual: una zona de drop visible, botón para pegar URL, indicador de progreso con etapas, manejo de errores y botón de cancelar. ContentView pasa de ser un `Text("Strata")` placeholder a una layout funcional con import + biblioteca.

Output: ImportView reutilizable, ContentView con layout principal, StrataApp con ImportViewModel en el environment.
</objective>

<execution_context>
@/Users/danielmartorell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmartorell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-import-end-to-end-flow/06-RESEARCH.md
@.planning/phases/06-import-end-to-end-flow/06-01-SUMMARY.md

@StrataClient/Import/ImportPhase.swift
@StrataClient/Import/ImportViewModel.swift
@StrataClient/App/StrataApp.swift
@StrataClient/ContentView.swift
@StrataClient/Library/LibraryStore.swift
@StrataClient/Library/SongEntry.swift
@StrataClient/Auth/AuthViewModel.swift
</context>

<interfaces>
<!-- Contratos de Plan 01 que ImportView consumirá -->

From StrataClient/Import/ImportPhase.swift:
```swift
enum ImportPhase: Equatable {
    case idle
    case validating
    case uploading
    case processing(stage: String)
    case ready(cached: Bool)
    case error(String)

    var displayLabel: String  // Texto en español para mostrar en UI
    var isActive: Bool        // true cuando hay un import en curso
}
```

From StrataClient/Import/ImportViewModel.swift:
```swift
@Observable @MainActor
final class ImportViewModel {
    private(set) var phase: ImportPhase  // Observable — la UI observa este campo
    func startFileImport(from fileURL: URL)
    func startURLImport(urlString: String)
    func cancel()
}
```

From StrataClient/Library/LibraryStore.swift:
```swift
@Observable @MainActor
final class LibraryStore {
    private(set) var songs: [SongEntry]
}
```

From StrataClient/Library/SongEntry.swift:
```swift
struct SongEntry: Codable, Identifiable, Sendable {
    let id: UUID
    let title: String
    let artist: String?
    let duration: Double
    let sourceURL: String?
    let fileName: String?
    let sourceHash: String
    let addedAt: Date
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ImportView — drop zone + paste + progress UI</name>
  <files>StrataClient/Import/ImportView.swift</files>
  <action>
Crear `StrataClient/Import/ImportView.swift`. La vista tiene dos partes: zona de drop (siempre visible) y barra de progreso (visible cuando `phase.isActive || phase == .error`).

```swift
import SwiftUI
import UniformTypeIdentifiers
import AppKit

struct ImportView: View {
    @Environment(ImportViewModel.self) private var importViewModel
    @State private var isDragTargeted = false

    var body: some View {
        VStack(spacing: 12) {
            dropZone
            if importViewModel.phase.isActive || isErrorOrReady {
                progressSection
            }
        }
        .padding()
    }

    // MARK: - Drop Zone

    private var dropZone: some View {
        ZStack {
            RoundedRectangle(cornerRadius: 12)
                .strokeBorder(
                    isDragTargeted ? Color.accentColor : Color.secondary.opacity(0.4),
                    style: StrokeStyle(lineWidth: 2, dash: [6, 4])
                )
                .background(
                    RoundedRectangle(cornerRadius: 12)
                        .fill(isDragTargeted ? Color.accentColor.opacity(0.08) : Color.clear)
                )
                .frame(height: 100)

            VStack(spacing: 6) {
                Image(systemName: "square.and.arrow.down")
                    .font(.system(size: 24))
                    .foregroundStyle(.secondary)
                Text("Arrastra un archivo de audio aquí")
                    .font(.subheadline)
                    .foregroundStyle(.secondary)
            }
        }
        .onDrop(of: [UTType.audio], isTargeted: $isDragTargeted) { providers in
            guard let provider = providers.first else { return false }
            _ = provider.loadFileRepresentation(forTypeIdentifier: UTType.audio.identifier) { url, _ in
                guard let url else { return }
                // Copiar a directorio temporal propiedad de la app ANTES de salir de la closure
                let tempCopy = FileManager.default.temporaryDirectory
                    .appendingPathComponent(UUID().uuidString + "." + url.pathExtension)
                try? FileManager.default.copyItem(at: url, to: tempCopy)
                Task { @MainActor in
                    importViewModel.startFileImport(from: tempCopy)
                }
            }
            return true
        }
    }

    // MARK: - Progress Section

    private var progressSection: some View {
        HStack(spacing: 10) {
            if importViewModel.phase.isActive {
                ProgressView()
                    .controlSize(.small)
            } else {
                statusIcon
            }

            Text(importViewModel.phase.displayLabel)
                .font(.subheadline)
                .foregroundStyle(isErrorPhase ? .red : .primary)
                .lineLimit(2)

            Spacer()

            if importViewModel.phase.isActive {
                Button("Cancelar") {
                    importViewModel.cancel()
                }
                .buttonStyle(.borderless)
                .foregroundStyle(.secondary)
            }
        }
        .padding(.horizontal, 4)
    }

    private var statusIcon: some View {
        Group {
            switch importViewModel.phase {
            case .ready:
                Image(systemName: "checkmark.circle.fill")
                    .foregroundStyle(.green)
            case .error:
                Image(systemName: "exclamationmark.circle.fill")
                    .foregroundStyle(.red)
            default:
                EmptyView()
            }
        }
    }

    private var isErrorPhase: Bool {
        if case .error = importViewModel.phase { return true }
        return false
    }

    private var isErrorOrReady: Bool {
        switch importViewModel.phase {
        case .ready, .error: return true
        default: return false
        }
    }
}
```

**Paste URL button:** Este botón se coloca en `ContentView` (no en ImportView) para que sea visible junto al título de la biblioteca. Exportar también una función helper en ImportView para leer el portapapeles:

```swift
// En ContentView — botón que llama a importViewModel.startURLImport
// (ver Task 2)
```

Añadir `ImportView.swift` al target Xcode en `project.pbxproj` o `project.yml`.
  </action>
  <verify>
    <automated>cd /Volumes/T7_SAMSUNG/strata && xcodebuild -scheme StrataClient -destination 'platform=macOS' build 2>&1 | tail -10</automated>
  </verify>
  <done>Build succeeded. ImportView compila con drop zone + progress section. El handler de onDrop copia el archivo antes de salir del closure (evita el pitfall de NSItemProvider security window).</done>
</task>

<task type="auto">
  <name>Task 2: ContentView layout + StrataApp ImportViewModel wiring</name>
  <files>StrataClient/ContentView.swift, StrataClient/App/StrataApp.swift</files>
  <action>
**StrataApp.swift:** Añadir `ImportViewModel` como `@State` en StrataApp. Crear la instancia con las dependencias (apiClient, cacheManager, libraryStore, authViewModel). Inyectar en el environment junto a los demás.

```swift
@main
struct StrataApp: App {
    @State private var authViewModel = AuthViewModel()
    @State private var libraryStore: LibraryStore
    @State private var importViewModel: ImportViewModel

    init() {
        let cacheManager = try! CacheManager()
        let store = LibraryStore(cacheManager: cacheManager)
        let auth = AuthViewModel()
        _authViewModel = State(initialValue: auth)
        _libraryStore = State(initialValue: store)
        _importViewModel = State(initialValue: ImportViewModel(
            apiClient: APIClient(),
            cacheManager: cacheManager,
            libraryStore: store,
            authViewModel: auth
        ))
    }

    var body: some Scene {
        WindowGroup {
            Group {
                if authViewModel.isAuthenticated {
                    ContentView()
                        .environment(libraryStore)
                        .environment(importViewModel)
                } else {
                    LoginView()
                }
            }
            .environment(authViewModel)
            .task {
                await libraryStore.loadFromDisk()
            }
        }
        .windowStyle(.titleBar)
        .defaultSize(width: 900, height: 600)
        .windowResizability(.contentMinSize)
    }
}
```

Nota: AuthViewModel se inicializa en `init()`, no con `@State private var authViewModel = AuthViewModel()` ya que ImportViewModel necesita la misma instancia. Ajustar para compartir la instancia — usar `State(initialValue:)` para todos.

**ContentView.swift:** Layout con dos secciones: panel de importación (arriba) y lista de biblioteca (abajo). Añadir botón "Pegar URL" en la barra de herramientas.

```swift
import SwiftUI

struct ContentView: View {
    @Environment(LibraryStore.self) private var libraryStore
    @Environment(ImportViewModel.self) private var importViewModel

    var body: some View {
        VStack(spacing: 0) {
            ImportView()
                .padding(.horizontal, 16)
                .padding(.top, 12)

            Divider()
                .padding(.top, 8)

            librarySection
        }
        .toolbar {
            ToolbarItem(placement: .primaryAction) {
                Button {
                    if let urlString = NSPasteboard.general.string(forType: .string) {
                        importViewModel.startURLImport(urlString: urlString)
                    }
                } label: {
                    Label("Pegar URL de YouTube", systemImage: "link")
                }
                .help("Pega una URL de YouTube del portapapeles (⌘V)")
                .disabled(importViewModel.phase.isActive)
            }
        }
    }

    private var librarySection: some View {
        Group {
            if libraryStore.songs.isEmpty {
                emptyLibraryPlaceholder
            } else {
                List(libraryStore.songs) { song in
                    songRow(song)
                }
                .listStyle(.inset)
            }
        }
    }

    private var emptyLibraryPlaceholder: some View {
        VStack(spacing: 8) {
            Spacer()
            Text("Biblioteca vacía")
                .font(.headline)
                .foregroundStyle(.secondary)
            Text("Arrastra un archivo o pega una URL de YouTube para importar")
                .font(.subheadline)
                .foregroundStyle(.tertiary)
                .multilineTextAlignment(.center)
            Spacer()
        }
        .padding()
    }

    private func songRow(_ song: SongEntry) -> some View {
        VStack(alignment: .leading, spacing: 2) {
            Text(song.title)
                .font(.body)
            if let artist = song.artist {
                Text(artist)
                    .font(.caption)
                    .foregroundStyle(.secondary)
            }
        }
        .padding(.vertical, 2)
    }
}
```
  </action>
  <verify>
    <automated>cd /Volumes/T7_SAMSUNG/strata && xcodebuild -scheme StrataClient -destination 'platform=macOS' build 2>&1 | tail -10</automated>
  </verify>
  <done>Build succeeded. StrataApp crea ImportViewModel con las dependencias correctas e inyecta en environment. ContentView muestra ImportView + lista de biblioteca + botón Pegar URL en toolbar. La app arranca correctamente.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificar UI de importación end-to-end</name>
  <what-built>
    ImportView con drag & drop zone, botón "Pegar URL de YouTube" en toolbar, barra de progreso con etapas, y biblioteca vacía con placeholder. ImportViewModel conectado via @Environment.
  </what-built>
  <how-to-verify>
    1. Abrir la app (xcodebuild o Xcode run)
    2. Verificar que la ventana muestra la zona de drop con borde punteado y texto "Arrastra un archivo de audio aquí"
    3. Verificar que la toolbar tiene el botón "Pegar URL de YouTube" (icono de link)
    4. Verificar que la sección inferior muestra "Biblioteca vacía" con texto explicativo
    5. Arrastrar un MP3 a la zona de drop — verificar que el borde cambia de color al hover
    6. Tras soltar: verificar que la UI pasa por "Validando..." → "Subiendo archivo..." → "En cola..." → etapas de procesamiento
    7. El botón "Cancelar" debe aparecer durante el proceso y al pulsarlo la UI vuelve a idle
    8. Copiar una URL de YouTube al portapapeles, clicar "Pegar URL de YouTube" — verificar que arranca el import URL
    9. Pegar una URL no-YouTube — verificar que muestra "Error: URL de YouTube no válida"
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o describe los problemas encontrados</resume-signal>
  <action>Verificar visualmente que la UI de importación funciona correctamente según los pasos descritos en how-to-verify.</action>
  <verify><automated>Manual — ver how-to-verify</automated></verify>
  <done>El usuario confirma que la UI de importación funciona correctamente end-to-end.</done>
</task>

</tasks>

<verification>
1. Build succeeded sin errores
2. `ImportView` tiene `.onDrop(of: [UTType.audio], ...)` con `loadFileRepresentation` iniciado dentro del closure `perform`
3. `ContentView` tiene `NSPasteboard.general.string(forType: .string)` en el botón de toolbar
4. `StrataApp` inyecta `importViewModel` en el environment para la rama autenticada
5. El botón "Pegar URL" está deshabilitado mientras `phase.isActive`
</verification>

<success_criteria>
- Build succeeded
- La zona de drop responde visualmente al drag (borde cambia de color)
- La barra de progreso muestra las etapas en español en tiempo real
- El botón Cancelar detiene el import
- El flujo URL muestra error "URL de YouTube no válida" para URLs incorrectas
- Checkpoint aprobado por el usuario
</success_criteria>

<output>
After completion, create `.planning/phases/06-import-end-to-end-flow/06-02-SUMMARY.md`
</output>
