---
phase: 02-gpu-pipeline
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app.py
  - server/processors/stub_processor.py
  - tests/test_error_propagation.py
autonomous: true
requirements: [PROC-06]
gap_closure: true

must_haves:
  truths:
    - "Cuando yt-dlp falla, el job pasa a status 'error' en lugar de quedarse en 'queued'"
    - "GET /result/{job_id} devuelve HTTP 500 con mensaje cuando status es 'error'"
    - "Si el FunctionCall de Modal lanza FunctionCallError, el endpoint lo captura y devuelve 500"
    - "process() también propaga el error al job_progress si cualquier etapa falla"
  artifacts:
    - path: "server/app.py"
      provides: "try/except en process_youtube() y process() que escribe 'error:...' en job_progress"
      contains: "f\"error:{e}\""
    - path: "server/processors/stub_processor.py"
      provides: "Rama 'error' en GET /result/{job_id} con HTTP 500 y captura FunctionCallError"
      contains: "status.startswith(\"error:\")"
    - path: "tests/test_error_propagation.py"
      provides: "Tests unitarios de propagación de errores (no integration)"
      exports:
        - test_process_youtube_sets_error_on_failure
        - test_process_sets_error_on_stage_failure
        - test_get_result_returns_500_on_error_status
        - test_get_result_catches_function_call_error
  key_links:
    - from: "AudioPipeline.process_youtube()"
      to: "modal.Dict strata-job-progress"
      via: "except Exception as e: progress[job_id] = f'error:{e}'"
      pattern: "progress\\[job_id\\] = f.error:"
    - from: "GET /result/{job_id}"
      to: "HTTP 500"
      via: "status.startswith('error:') branch"
      pattern: "status\\.startswith.*error"
---

<objective>
Cerrar el gap de propagación de errores del pipeline YouTube (UAT Test 5). Actualmente, cuando yt-dlp falla (bloqueado por bot detection), el job se queda en status "queued" para siempre porque process_youtube() no tiene try/except. El endpoint GET /result/{job_id} tampoco tiene rama para status "error". El usuario nunca recibe feedback de fallo.

Purpose: Que el usuario reciba un HTTP 500 con mensaje claro en vez de un job bloqueado en "queued" indefinidamente.
Output: server/app.py con try/except en process_youtube() y process(); stub_processor.py con rama "error" y captura FunctionCallError; tests unitarios verificando todo.
</objective>

<execution_context>
@/Users/danielmartorell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmartorell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-gpu-pipeline/02-UAT.md

<interfaces>
<!-- Contratos actuales. Extraídos del código en server/app.py y server/processors/stub_processor.py -->

AudioPipeline.process_youtube() — líneas 329-352 de server/app.py:
```python
@modal.method()
def process_youtube(self, url: str, username: str) -> bytes:
    import sys
    if "/root" not in sys.path:
        sys.path.insert(0, "/root")
    from pipeline.downloader import download_youtube_audio
    audio_bytes, yt_metadata = download_youtube_audio(url, "/tmp")
    return self.process.local(
        audio_bytes, "youtube", url, username, youtube_metadata=yt_metadata,
    )
```
BUG: Sin try/except. Si download_youtube_audio() lanza, job_progress se queda en "queued".
NOTA: process_youtube() no obtiene job_id — hay que añadir modal.current_function_call_id() y modal.Dict.from_name().

AudioPipeline.process() — líneas 240-327 de server/app.py:
```python
@modal.method()
def process(self, audio_bytes, source_type, source_name, username, youtube_metadata=None) -> bytes:
    job_id = modal.current_function_call_id()
    progress = modal.Dict.from_name("strata-job-progress", create_if_missing=True)
    validate_audio(audio_bytes)           # fuera del try (error esperado, va al HTTP handler)
    progress[job_id] = "separating"
    stems = separate_stems(...)
    progress[job_id] = "transcribing"
    lyrics = transcribe_vocals(...)
    progress[job_id] = "detecting_chords"
    chords = detect_chords(...)
    progress[job_id] = "packaging"
    result = package_results(...)
    progress[job_id] = "completed"
    return result
```
BUG: Sin try/except global. Si separate_stems() falla, progress se queda en "separating".

GET /result/{job_id} — líneas 102-133 de server/processors/stub_processor.py:
```python
@router.get("/result/{job_id}")
async def get_result(job_id: str, username: str = Depends(require_auth)):
    import modal
    job_dict = _get_job_dict()
    try:
        status = job_dict.get(job_id, "unknown")
    except Exception:
        status = "unknown"

    if status != "completed":
        return {"status": status if status != "unknown" else "processing", "job_id": job_id}

    try:
        call = modal.FunctionCall.from_id(job_id)
        result = await call.get.aio(timeout=0)
        return Response(content=result, media_type="application/zip", ...)
    except TimeoutError:
        return {"status": "processing", "job_id": job_id}
    except modal.exception.OutputExpiredError:
        raise HTTPException(status_code=404, detail="Resultado expirado")
```
BUG 1: No hay rama para status que empiece por "error:" — devuelve {"status": "error:...", "job_id": ...} con HTTP 200.
BUG 2: No captura modal.exception.FunctionCallError en call.get.aio().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests unitarios de propagación de errores (RED)</name>
  <files>tests/test_error_propagation.py</files>
  <behavior>
    - test_process_youtube_sets_error_on_failure: mock download_youtube_audio para lanzar RuntimeError("yt-dlp blocked") — tras el fix, progress debe contener "error:yt-dlp blocked" y la excepción debe re-lanzarse
    - test_process_sets_error_on_stage_failure: mock separate_stems para lanzar RuntimeError("cuda OOM") — tras el fix, progress debe contener "error:cuda OOM" y la excepción debe re-lanzarse
    - test_get_result_returns_500_on_error_status: simular job_dict con status "error:yt-dlp blocked" — tras el fix, el endpoint debe devolver HTTP 500 con detail que contiene ese mensaje
    - test_get_result_catches_function_call_error: simular status "completed" pero call.get.aio() lanza FunctionCallError — tras el fix, el endpoint debe devolver HTTP 500
    - TODOS son tests unitarios locales, NO requieren Modal token ni deploys
  </behavior>
  <action>
    Crear /Volumes/T7_SAMSUNG/strata/tests/test_error_propagation.py.

    Estructura del archivo:
    ```python
    """Tests unitarios de propagación de errores en AudioPipeline y GET /result/{job_id}.
    No requieren Modal token — usan mocks de unittest.mock.
    """
    import pytest
    from unittest.mock import MagicMock, patch, AsyncMock
    from fastapi.testclient import TestClient
    ```

    TEST 1 — process_youtube error propagation:
    Parchear `modal.current_function_call_id` para devolver "test-job-id".
    Parchear `modal.Dict.from_name` para devolver un dict-like mock.
    Parchear `pipeline.downloader.download_youtube_audio` para lanzar RuntimeError("yt-dlp blocked").
    Instanciar AudioPipeline (sin Modal context — usar import directo de server/app.py).
    Llamar process_youtube.local(url, username) o simular la lógica directamente.
    Verificar que el mock dict recibió `__setitem__("test-job-id", ...)` con valor que empieza por "error:".

    TEST 2 — process error propagation:
    Similar, pero parchear `pipeline.separation.separate_stems` para lanzar RuntimeError("cuda OOM").
    Verificar que progress recibió valor que empieza por "error:".

    TEST 3 — get_result HTTP 500 on error status:
    Importar el router de stub_processor y crear TestClient de FastAPI.
    Override de require_auth para devolver "test_user".
    Mock de _get_job_dict() para devolver dict con job_id -> "error:yt-dlp blocked".
    GET /result/test-job-id debe devolver 500 con body que contiene "yt-dlp blocked".

    TEST 4 — get_result captura FunctionCallError:
    Mock de _get_job_dict() para devolver "completed".
    Mock de modal.FunctionCall.from_id().get.aio para lanzar Exception con nombre "FunctionCallError".
    GET /result/test-job-id debe devolver 500 (no 500 sin capturar).

    Nota sobre imports de Modal en tests locales: importar modal puede fallar si no hay token.
    Proteger con `try: import modal; FunctionCallError = modal.exception.FunctionCallError except: class FunctionCallError(Exception): pass`.

    Ejecutar: `python -m pytest tests/test_error_propagation.py -v` — DEBEN FALLAR en esta fase (RED).
  </action>
  <verify>
    <automated>cd /Volumes/T7_SAMSUNG/strata && python -m pytest tests/test_error_propagation.py -v --no-header 2>&1 | tail -20</automated>
  </verify>
  <done>4 tests creados. Pueden fallar por ImportError si modal no importa localmente — en ese caso adaptar los mocks. Lo importante: los tests describen el comportamiento esperado y FALLAN por ausencia de la funcionalidad (no por errores de setup).</done>
</task>

<task type="auto">
  <name>Task 2: Añadir try/except en process_youtube() — server/app.py</name>
  <files>server/app.py</files>
  <action>
    Modificar el método process_youtube() de AudioPipeline en /Volumes/T7_SAMSUNG/strata/server/app.py.

    ANTES (líneas ~329-352):
    ```python
    @modal.method()
    def process_youtube(self, url: str, username: str) -> bytes:
        import sys
        if "/root" not in sys.path:
            sys.path.insert(0, "/root")

        from pipeline.downloader import download_youtube_audio
        audio_bytes, yt_metadata = download_youtube_audio(url, "/tmp")
        return self.process.local(
            audio_bytes,
            "youtube",
            url,
            username,
            youtube_metadata=yt_metadata,
        )
    ```

    DESPUÉS:
    ```python
    @modal.method()
    def process_youtube(self, url: str, username: str) -> bytes:
        import sys
        if "/root" not in sys.path:
            sys.path.insert(0, "/root")

        job_id = modal.current_function_call_id()
        progress = modal.Dict.from_name("strata-job-progress", create_if_missing=True)

        try:
            from pipeline.downloader import download_youtube_audio
            audio_bytes, yt_metadata = download_youtube_audio(url, "/tmp")
            return self.process.local(
                audio_bytes,
                "youtube",
                url,
                username,
                youtube_metadata=yt_metadata,
            )
        except Exception as e:
            progress[job_id] = f"error:{e}"
            raise
    ```

    También modificar process() para añadir try/except global DESPUÉS de validate_audio().

    ANTES en process() (tras validate_audio, línea ~277 aprox):
    ```python
        # Step 1: Separar stems (Demucs htdemucs)
        progress[job_id] = "separating"
        ...
        progress[job_id] = "completed"
        return result
    ```

    DESPUÉS — envolver todas las etapas tras validate_audio en try/except:
    ```python
        try:
            # Step 1: Separar stems (Demucs htdemucs)
            progress[job_id] = "separating"
            from pipeline.separation import separate_stems
            stems = separate_stems(self.demucs_model, audio_bytes)
            # ... resto de etapas sin cambio ...
            progress[job_id] = "completed"
            return result
        except Exception as e:
            progress[job_id] = f"error:{e}"
            raise
    ```

    validate_audio() debe quedar FUERA del try/except (su ValueError es un error de validación esperado que el HTTP handler ya captura como 400, no como error de pipeline).

    Verificar sintaxis: `python -c "import ast; ast.parse(open('server/app.py').read()); print('OK')"`
  </action>
  <verify>
    <automated>cd /Volumes/T7_SAMSUNG/strata/server && python -c "import ast; ast.parse(open('app.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>server/app.py actualizado. process_youtube() y process() tienen try/except que escribe "error:{e}" en job_progress. validate_audio() queda fuera del try en process(). Sintaxis válida.</done>
</task>

<task type="auto">
  <name>Task 3: Rama "error" y FunctionCallError en GET /result/{job_id}</name>
  <files>server/processors/stub_processor.py</files>
  <action>
    Modificar get_result() en /Volumes/T7_SAMSUNG/strata/server/processors/stub_processor.py.

    Localizar el bloque actual (tras obtener status):
    ```python
    if status != "completed":
        return {"status": status if status != "unknown" else "processing", "job_id": job_id}
    ```

    Reemplazar por:
    ```python
    # Error propagado desde el pipeline (formato "error:{mensaje}")
    if isinstance(status, str) and status.startswith("error:"):
        error_detail = status[len("error:"):]
        raise HTTPException(status_code=500, detail=f"Pipeline error: {error_detail}")

    # Error sin mensaje adjunto
    if status == "error":
        raise HTTPException(status_code=500, detail="Pipeline failed with unknown error")

    if status != "completed":
        return {"status": status if status != "unknown" else "processing", "job_id": job_id}
    ```

    En el bloque try/except de call.get.aio(), añadir antes de los except existentes:
    ```python
    try:
        call = modal.FunctionCall.from_id(job_id)
        result = await call.get.aio(timeout=0)
        return Response(
            content=result,
            media_type="application/zip",
            headers={"Content-Disposition": "attachment; filename=result.zip"},
        )
    except modal.exception.FunctionCallError as e:
        raise HTTPException(status_code=500, detail=f"Pipeline error: {e}")
    except TimeoutError:
        return {"status": "processing", "job_id": job_id}
    except modal.exception.OutputExpiredError:
        raise HTTPException(status_code=404, detail="Resultado expirado")
    ```

    Verificar con suite completa de tests unitarios:
    `python -m pytest tests/ -m "not integration" -v`
  </action>
  <verify>
    <automated>cd /Volumes/T7_SAMSUNG/strata && python -m pytest tests/ -m "not integration" -v --no-header 2>&1 | tail -30</automated>
  </verify>
  <done>Todos los tests unitarios pasan (19+ tests previos + 4 nuevos de test_error_propagation). GET /result/{job_id} devuelve HTTP 500 cuando status empieza por "error:" o cuando FunctionCallError es lanzado. Sin regresiones.</done>
</task>

</tasks>

<verification>
Suite completa de tests unitarios:
```bash
cd /Volumes/T7_SAMSUNG/strata && python -m pytest tests/ -m "not integration" -v --no-header
```
Debe mostrar 23+ tests pasando (19 previos + 4 nuevos).

Verificación de sintaxis de ambos archivos modificados:
```bash
cd /Volumes/T7_SAMSUNG/strata/server && python -c "
import ast
ast.parse(open('app.py').read())
ast.parse(open('processors/stub_processor.py').read())
print('Both files: syntax OK')
"
```
</verification>

<success_criteria>
- process_youtube() escribe "error:{msg}" en job_progress cuando download_youtube_audio() falla
- process() escribe "error:{msg}" en job_progress cuando cualquier etapa (separation, transcription, chords, packaging) falla
- GET /result/{job_id} devuelve HTTP 500 con el mensaje de error cuando status empieza por "error:"
- GET /result/{job_id} captura modal.exception.FunctionCallError y devuelve HTTP 500
- validate_audio() queda fuera del try/except en process() (su ValueError va al HTTP handler como 400, no como error de pipeline)
- Todos los tests unitarios previos siguen pasando (sin regresiones)
- UAT Test 5 pasa: el usuario ya no ve un job bloqueado en "queued" cuando YouTube bloquea la descarga
</success_criteria>

<output>
Tras completar el plan, crear `/Volumes/T7_SAMSUNG/strata/.planning/phases/02-gpu-pipeline/02-gap-SUMMARY.md` con:
- Gaps cerrados (UAT Test 5)
- Archivos modificados y cambios clave
- Tests añadidos
- Comportamiento antes/después
</output>
