---
phase: 03-swift-client-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/auth/auth.py
  - StrataClient/Auth/KeychainService.swift
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "KeychainService puede guardar un JWT String en Keychain sin errores"
    - "KeychainService puede leer el JWT guardado y devolver el String exacto"
    - "KeychainService puede eliminar el JWT del Keychain"
    - "KeychainService hace upsert: si ya existe una entrada, la actualiza en vez de fallar con duplicado"
    - "El servidor responde 200 con nuevo JWT a POST /auth/renew con token válido en Authorization: Bearer"
    - "El servidor responde 401 a POST /auth/renew con token expirado o inválido"
  artifacts:
    - path: "StrataClient/Auth/KeychainService.swift"
      provides: "Security.framework wrapper para JWT (save/load/delete)"
      exports: ["KeychainService", "KeychainError"]
    - path: "server/auth/auth.py"
      provides: "Endpoint POST /auth/renew — renovación stateless de JWT"
      contains: "@router.post(\"/renew\")"
  key_links:
    - from: "KeychainService.saveToken"
      to: "Security.framework SecItemUpdate / SecItemAdd"
      via: "upsert pattern (update primero, add si errSecItemNotFound)"
      pattern: "SecItemUpdate.*errSecItemNotFound.*SecItemAdd"
    - from: "POST /auth/renew (servidor)"
      to: "jwt.decode + jwt.encode"
      via: "require_auth dependency para validar token entrante"
      pattern: "router.post.*renew"
---

<objective>
Dos implementaciones independientes que se completan en paralelo con 03-01:
1. `KeychainService.swift` — wrapper de Security.framework para persistir el JWT en Keychain
2. Endpoint `POST /auth/renew` en el servidor FastAPI — renovación stateless de tokens

Purpose: La sesión de 90 días (AUTH-02) requiere persistencia en Keychain. La renovación silenciosa (AUTH-03) requiere el endpoint de servidor. Ambos son precondiciones de AuthViewModel (plan 03-03).
Output: KeychainService listo para inyectarse en AuthViewModel; endpoint /auth/renew desplegable junto con el resto de la API.
</objective>

<execution_context>
@/Users/danielmartorell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmartorell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-swift-client-auth/03-CONTEXT.md
@.planning/phases/03-swift-client-auth/03-RESEARCH.md

<interfaces>
<!-- server/auth/auth.py — contrato actual del servidor -->
<!-- JWT_EXPIRY_DAYS = 90, algoritmo HS256, payload: {sub, iat, exp} -->
<!-- Dependencia require_auth: valida JWT Bearer y devuelve username (str) -->
<!-- Respuesta de login: {"token": "<jwt>", "expires_in": 7776000} -->
<!-- MISMO schema para /auth/renew -->

<!-- KeychainService se usa en AuthViewModel así: -->
<!--   let jwt = try keychain.loadToken()          -->
<!--   try keychain.saveToken(newJWT)              -->
<!--   try keychain.deleteToken()                  -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KeychainService — Security.framework wrapper</name>
  <files>
    StrataClient/Auth/KeychainService.swift
    StrataClientTests/KeychainServiceTests.swift
  </files>
  <behavior>
    - saveToken(_:) guarda un JWT String como kSecClassGenericPassword con service="com.strata.client", account="jwt-token"
    - saveToken(_:) segunda vez actualiza el item existente (upsert — no lanza errSecDuplicateItem)
    - loadToken() devuelve el String exacto que se guardó
    - loadToken() lanza KeychainError.itemNotFound si no hay token guardado
    - deleteToken() elimina el item del Keychain
    - deleteToken() no lanza error si no hay item que eliminar (errSecItemNotFound se ignora)
    - Todos los OSStatus != errSecSuccess lanzan KeychainError.unexpectedStatus(status)
  </behavior>
  <action>
**KeychainService.swift** — usar exactamente el patrón del RESEARCH (upsert: SecItemUpdate primero, SecItemAdd si errSecItemNotFound):

```swift
import Security
import Foundation

final class KeychainService {
    private let service = "com.strata.client"
    private let account = "jwt-token"

    enum KeychainError: Error {
        case itemNotFound
        case invalidFormat
        case unexpectedStatus(OSStatus)
    }

    func saveToken(_ token: String) throws { ... }   // upsert
    func loadToken() throws -> String { ... }
    func deleteToken() throws { ... }
}
```

Parámetro kSecAttrAccessible: usar kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly — accesible tras primer desbloqueo, no migra a otros dispositivos (apropiado para JWT de sesión).

Tests en KeychainServiceTests.swift: probar save→load, double-save→load (upsert), load sin token (error), delete→load (error), delete sin token (no error). Los tests usan el Keychain real del entorno de test — no mockeado (Security.framework no tiene mock oficial y el comportamiento real es lo que importa).

NOTA: Si el test runner ejecuta con firma ad-hoc, los tests de Keychain pueden fallar con errSecAuthFailed. Documentar este caso como expected en un comentario en el test file. El ejecutor debe asegurarse de que el target de tests tiene la misma signing identity "Apple Development".
  </action>
  <verify>
    <automated>xcodebuild test -project /Volumes/T7_SAMSUNG/strata/StrataClient/StrataClient.xcodeproj -scheme StrataClient -destination 'platform=macOS' -only-testing:StrataClientTests/KeychainServiceTests 2>&1 | grep -E "Test Suite|passed|failed"</automated>
  </verify>
  <done>Todos los tests de KeychainServiceTests pasan. KeychainService exporta save/load/delete. KeychainError está definido.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Añadir POST /auth/renew al servidor</name>
  <files>
    server/auth/auth.py
    server/tests/test_auth.py
  </files>
  <behavior>
    - POST /auth/renew con JWT válido en Authorization: Bearer devuelve 200 con {"token": "<nuevo_jwt>", "expires_in": 7776000}
    - El nuevo JWT tiene exp = ahora + 90 días (renovación completa, no extensión)
    - El nuevo JWT tiene el mismo "sub" (username) que el token entrante
    - POST /auth/renew con token expirado devuelve 401 (require_auth ya maneja esto)
    - POST /auth/renew sin Authorization header devuelve 401
    - POST /auth/renew con token con firma inválida devuelve 401
  </behavior>
  <action>
Añadir endpoint a `server/auth/auth.py` dentro del `router` existente:

```python
@router.post("/renew")
def renew_token(username: str = Depends(require_auth)):
    """Renueva el JWT del usuario autenticado.

    El cliente envía el JWT vigente en Authorization: Bearer.
    require_auth lo valida y devuelve el username.
    Se emite un nuevo JWT con 90 días desde ahora — renovación stateless.
    """
    now = datetime.now(timezone.utc)
    exp = now + timedelta(days=JWT_EXPIRY_DAYS)
    payload = {
        "sub": username,
        "iat": int(now.timestamp()),
        "exp": int(exp.timestamp()),
    }
    token = jwt.encode(payload, JWT_SECRET, algorithm=JWT_ALGORITHM)
    return {"token": token, "expires_in": JWT_EXPIRY_DAYS * 24 * 3600}
```

Añadir tests a `server/tests/test_auth.py` (o crear el archivo si no existe). Usar `TestClient` de FastAPI con la app de test (sin Modal). Los tests deben cubrir:
- Renovación exitosa con token válido → 200 + nuevo "token"
- Renovación con token expirado → 401
- Renovación sin header → 401
- Renovación con token corrupto → 401

Para construir el token de test válido: usar `jwt.encode({"sub": "papa", "iat": ..., "exp": ...}, JWT_SECRET, "HS256")` directamente (no llamar a /login).
  </action>
  <verify>
    <automated>cd /Volumes/T7_SAMSUNG/strata && python -m pytest server/tests/test_auth.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>Todos los tests de test_auth.py pasan. POST /auth/renew existe en auth.py y devuelve {"token": ...} con 200 para token válido y 401 para token inválido/ausente.</done>
</task>

</tasks>

<verification>
1. KeychainServiceTests pasan: save, upsert, load, load-not-found, delete, delete-not-found
2. `pytest server/tests/test_auth.py` pasa: renew-ok, renew-expired, renew-no-header, renew-corrupt
3. server/auth/auth.py contiene `@router.post("/renew")`
4. La respuesta de /auth/renew tiene el campo "token" (mismo schema que /auth/login)
5. KeychainService.swift exporta `KeychainService` y `KeychainError`
</verification>

<success_criteria>
- KeychainService hace upsert correcto — no falla en segunda escritura
- Endpoint /auth/renew es stateless: solo necesita el JWT entrante, no base de datos adicional
- require_auth existente se reutiliza para validar el token entrante en /auth/renew (no duplicar lógica de validación)
</success_criteria>

<output>
After completion, create `.planning/phases/03-swift-client-auth/03-02-SUMMARY.md`
</output>
