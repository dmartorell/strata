---
phase: 03-swift-client-auth
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - StrataClient/App/StrataApp.swift
  - StrataClient/Auth/AuthViewModel.swift
  - StrataClient/Auth/LoginView.swift
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - "El usuario introduce la contraseña, pulsa Entrar y la app pasa a ContentView sin volver a pedir login"
    - "Al relanzar la app con sesión vigente en Keychain, no aparece LoginView"
    - "Al relanzar la app con token expirado en Keychain, aparece LoginView y el token se borra"
    - "Cuando el token tiene < 7 días de vida, se renueva silenciosamente sin que el usuario lo note"
    - "Un 401 recibido desde cualquier endpoint de APIClient llama a authViewModel.logout() y aparece LoginView"
    - "LoginView muestra solo SecureField de contraseña (sin campo de usuario)"
    - "Durante el login en curso se muestra ProgressView y el botón queda deshabilitado"
    - "Cualquier error de login muestra el mensaje genérico 'Contraseña incorrecta' sin distinguir red de credenciales"
  artifacts:
    - path: "StrataClient/Auth/AuthViewModel.swift"
      provides: "@Observable fuente única de verdad para isAuthenticated y token"
      exports: ["AuthViewModel"]
    - path: "StrataClient/Auth/LoginView.swift"
      provides: "Pantalla de login: SecureField + botón + ProgressView + error"
      exports: ["LoginView"]
    - path: "StrataClient/App/StrataApp.swift"
      provides: "Root auth gate: isAuthenticated → ContentView o LoginView"
  key_links:
    - from: "StrataApp.swift"
      to: "AuthViewModel.isAuthenticated"
      via: "@Observable @State + if/else en WindowGroup"
      pattern: "authViewModel.isAuthenticated"
    - from: "AuthViewModel.init"
      to: "KeychainService.loadToken"
      via: "checkStoredToken() síncrono en init"
      pattern: "checkStoredToken"
    - from: "AuthViewModel"
      to: "APIClient.renewToken"
      via: "Task { try? await silentRenew() } disparado si quedan < 7 días"
      pattern: "silentRenew"
    - from: "APIClient (401 response)"
      to: "authViewModel.logout()"
      via: "el llamador de APIClient captura APIError.unauthorized y llama logout"
      pattern: "APIError.unauthorized.*logout"

user_setup:
  - service: "apple-developer-account"
    why: "Keychain ACL requiere identidad de firma estable. Ad-hoc (Sign to Run Locally) rompe el Keychain entre rebuilds con errSecAuthFailed -25293."
    dashboard_config:
      - task: "Verificar que Xcode tiene una cuenta Apple ID añadida en Settings → Accounts"
        location: "Xcode → Settings → Accounts → (+) Apple ID"
      - task: "En el proyecto Xcode, Signing & Capabilities → Team: seleccionar tu Apple ID (Personal Team)"
        location: "Xcode → StrataClient target → Signing & Capabilities"
---

<objective>
Conectar todas las piezas: `AuthViewModel` (@Observable) como controlador de sesión, `LoginView` como UI de entrada, y `StrataApp` como root auth gate que alterna entre LoginView y ContentView según `isAuthenticated`.

Purpose: Completar el flujo de autenticación de extremo a extremo — login, persistencia 90 días, renovación silenciosa y redirect en expirado.
Output: App macOS funcional donde el usuario se autentica una sola vez y la sesión persiste.
</objective>

<execution_context>
@/Users/danielmartorell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmartorell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-swift-client-auth/03-CONTEXT.md
@.planning/phases/03-swift-client-auth/03-RESEARCH.md
@.planning/phases/03-swift-client-auth/03-01-SUMMARY.md
@.planning/phases/03-swift-client-auth/03-02-SUMMARY.md

<interfaces>
<!-- De 03-01: APIClient -->
struct APIClient {
    func login(password: String) async throws -> String        // devuelve JWT
    func renewToken(current: String) async throws -> String    // devuelve nuevo JWT
    func uploadAudio(fileData: Data, fileName: String, mimeType: String, token: String) async throws -> String
    func pollJobStatus(jobId: String, token: String) async throws -> JobResult
    func getUsage(token: String) async throws -> UsageResponse
}
enum APIError: Error { case unauthorized, timeout, processingFailed(String), httpError(Int), ... }

<!-- De 03-02: KeychainService -->
final class KeychainService {
    func saveToken(_ token: String) throws
    func loadToken() throws -> String
    func deleteToken() throws
    enum KeychainError: Error { case itemNotFound, invalidFormat, unexpectedStatus(OSStatus) }
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AuthViewModel — controlador de sesión @Observable</name>
  <files>
    StrataClient/Auth/AuthViewModel.swift
    StrataClientTests/AuthViewModelTests.swift
  </files>
  <behavior>
    - AuthViewModel.init() con token válido en Keychain: isAuthenticated = true, token != nil
    - AuthViewModel.init() sin token en Keychain: isAuthenticated = false, token = nil
    - AuthViewModel.init() con token expirado en Keychain: isAuthenticated = false, token = nil, Keychain limpio
    - AuthViewModel.init() con token que expira en < 7 días: llama silentRenew (token renovado en Keychain)
    - AuthViewModel.login(password:) exitoso: isAuthenticated = true, token guardado en Keychain
    - AuthViewModel.login(password:) fallido: lanza el error tal cual (LoginView lo convierte a mensaje genérico)
    - AuthViewModel.logout(): isAuthenticated = false, token = nil, Keychain limpio
    - AuthViewModel.handleUnauthorized(): equivalente a logout() — llamado por vistas cuando reciben APIError.unauthorized
  </behavior>
  <action>
**AuthViewModel.swift** — usar @Observable (macOS 14), NO ObservableObject:

```swift
import Observation
import JWTDecode

@Observable
final class AuthViewModel {
    private(set) var isAuthenticated: Bool = false
    private(set) var token: String? = nil

    private let keychain: KeychainService
    private let apiClient: APIClient
    private var isRenewing: Bool = false  // flag para evitar renovaciones paralelas

    init(keychain: KeychainService = KeychainService(),
         apiClient: APIClient = APIClient()) {
        self.keychain = keychain
        self.apiClient = apiClient
        checkStoredToken()
    }

    func login(password: String) async throws { ... }
    func logout() { ... }
    func handleUnauthorized() { logout() }

    private func checkStoredToken() { ... }   // síncrono, llamado en init
    private func isExpired(_ token: String) -> Bool { ... }
    private func needsRenewal(_ token: String) -> Bool { ... }  // < 7 días para expirar
    private func scheduleRenewalIfNeeded(for token: String) { ... }
    private func silentRenew() async throws { ... }
}
```

Umbral de renovación: 7 * 24 * 3600 segundos (7 días).

Race condition: usar el flag `isRenewing` (no actor — la simplicidad es suficiente para 2 usuarios). En `scheduleRenewalIfNeeded`: `guard !isRenewing else { return }; isRenewing = true; defer { isRenewing = false }`.

Tests: usar mocks de KeychainService y APIClient inyectados por init (inyección de dependencias). Los tests no tocan Keychain real ni red real. Cubrir todos los comportamientos del <behavior>.

Implementar KeychainServiceProtocol y APIClientProtocol (o usar `any` con los tipos concretos en tests via subclassing/stubbing — lo que el ejecutor considere más idiomático en Swift 5.9+).
  </action>
  <verify>
    <automated>xcodebuild test -project /Volumes/T7_SAMSUNG/strata/StrataClient/StrataClient.xcodeproj -scheme StrataClient -destination 'platform=macOS' -only-testing:StrataClientTests/AuthViewModelTests 2>&1 | grep -E "Test Suite|passed|failed"</automated>
  </verify>
  <done>Todos los tests de AuthViewModelTests pasan. AuthViewModel es @Observable. isAuthenticated y token son private(set).</done>
</task>

<task type="auto">
  <name>Task 2: LoginView + StrataApp root auth gate</name>
  <files>
    StrataClient/Auth/LoginView.swift
    StrataClient/App/StrataApp.swift
  </files>
  <action>
**LoginView.swift** — SwiftUI, macOS 14, solo contraseña:

```swift
struct LoginView: View {
    @Environment(AuthViewModel.self) private var auth
    @State private var password = ""
    @State private var isLoading = false
    @State private var errorMessage: String?

    var body: some View {
        VStack(spacing: 20) {
            Text("Strata").font(.largeTitle).fontWeight(.semibold)
            SecureField("Contraseña", text: $password)
                .frame(width: 280)
                .textFieldStyle(.roundedBorder)
            if let error = errorMessage {
                Text(error).foregroundStyle(.red).font(.caption)
            }
            Button(action: attemptLogin) {
                if isLoading {
                    ProgressView().scaleEffect(0.8)
                } else {
                    Text("Entrar").frame(width: 80)
                }
            }
            .buttonStyle(.borderedProminent)
            .disabled(password.isEmpty || isLoading)
            .keyboardShortcut(.return, modifiers: [])  // Enter dispara login
        }
        .padding(40)
        .frame(width: 360)
    }

    private func attemptLogin() {
        isLoading = true
        errorMessage = nil
        Task {
            do {
                try await auth.login(password: password)
            } catch {
                errorMessage = "Contraseña incorrecta"
            }
            isLoading = false
        }
    }
}
```

No campo de usuario — solo SecureField de contraseña. Error genérico "Contraseña incorrecta" para cualquier fallo (no distinguir red de credenciales, per CONTEXT.md).

**StrataApp.swift** — root auth gate con @Observable pattern (macOS 14):

```swift
@main
struct StrataApp: App {
    @State private var authViewModel = AuthViewModel()

    var body: some Scene {
        WindowGroup {
            if authViewModel.isAuthenticated {
                ContentView()
                    .environment(authViewModel)
            } else {
                LoginView()
                    .environment(authViewModel)
            }
        }
        .windowStyle(.titleBar)
        .windowResizability(.contentSize)
    }
}
```

IMPORTANTE: `@State private var authViewModel` (NO `@StateObject` — ese es el patrón legacy de ObservableObject). El switch LoginView/ContentView se hace directamente con if/else en body — NO usar `.sheet()` ni NavigationStack para el auth gate.

WindowGroup size: la ventana de login puede ser pequeña (360x280). ContentView definirá su propio tamaño mínimo en fases posteriores.

Smoke test manual en checkpoint siguiente.
  </action>
  <verify>
    <automated>xcodebuild -project /Volumes/T7_SAMSUNG/strata/StrataClient/StrataClient.xcodeproj -scheme StrataClient -destination 'platform=macOS' build 2>&1 | tail -3</automated>
  </verify>
  <done>El proyecto compila sin errores ni warnings. LoginView muestra SecureField sin campo de usuario. StrataApp usa @State con AuthViewModel (no @StateObject).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificar flujo de autenticación end-to-end</name>
  <what-built>
    Flujo completo de autenticación: LoginView → AuthViewModel → APIClient → Keychain → ContentView.
    - Proyecto Xcode firmado con "Apple Development"
    - login() guarda JWT en Keychain y pasa a ContentView
    - Al relanzar la app, el token del Keychain restaura la sesión automáticamente
    - Token expirado redirige a LoginView al arrancar
    - Renovación silenciosa configurada si quedan &lt; 7 días
  </what-built>
  <how-to-verify>
    1. Abrir StrataClient.xcodeproj en Xcode
    2. Verificar: Signing & Capabilities → Team es tu Apple ID (no "Sign to Run Locally")
    3. Ejecutar la app (Cmd+R) — debe aparecer LoginView con solo campo "Contraseña"
    4. Introducir una contraseña incorrecta → debe aparecer "Contraseña incorrecta" en rojo
    5. Introducir una contraseña correcta (configurada en el servidor) → debe aparecer ContentView
    6. Cerrar y relanzar la app → debe aparecer directamente ContentView (sesión persistida)
    7. En Xcode console, verificar que no hay errores de Keychain (OSStatus -25293 indicaría problema de firma)
    8. (Opcional) Forzar expiración: modificar temporalmente el check de isExpired para siempre true → relanzar → debe aparecer LoginView
  </how-to-verify>
  <resume-signal>Escribe "aprobado" si los 6 pasos funcionan correctamente, o describe qué falla</resume-signal>
  <action>Ejecutar la app en Xcode y seguir los pasos de verificación manual descritos en how-to-verify.</action>
  <verify>El usuario confirma "aprobado" en el resume-signal.</verify>
  <done>Login funciona con contraseña correcta, falla con contraseña incorrecta, y la sesión persiste entre relanzamientos.</done>
</task>

</tasks>

<verification>
1. `xcodebuild build` → BUILD SUCCEEDED en macOS 14
2. AuthViewModelTests todos pasan (init con token, init sin token, init expirado, login, logout, handleUnauthorized)
3. LoginView no tiene campo de usuario — solo SecureField
4. StrataApp usa `@State private var authViewModel` (no @StateObject)
5. Checkpoint humano aprobado: login real funciona, sesión persiste entre relanzamientos
</verification>

<success_criteria>
- El usuario puede hacer login una vez y no volver a ver LoginView en los próximos 90 días
- Al relanzar la app, el token del Keychain restaura la sesión sin red (checkStoredToken es síncrono)
- La renovación silenciosa usa el endpoint /auth/renew implementado en 03-02
- La fase 3 completa los 4 requisitos AUTH-01, AUTH-02, AUTH-03, AUTH-04
</success_criteria>

<output>
After completion, create `.planning/phases/03-swift-client-auth/03-03-SUMMARY.md`
</output>
